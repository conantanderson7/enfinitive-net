{
  "name": "enfinitive-net",
  "user": "ubuntu",
  "terminals": [
    {
      "name": "static-server",
      "command": "cd /workspace && python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Serves the static site over HTTP on port 8000. Open http://localhost:8000/ to preview pages."
    }
  ],
  "ports": [
    {
      "name": "static site",
      "port": 8000
    }
  ]
}
